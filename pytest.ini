[pytest]
addopts = -x -s --cov-report=xml --cov-report=term --cov=skin_doctor
# add '-m "wip"' to focus on tests marked with "wip" (@pytest.mark.wip)
markers = 
    wip: marks tests as work in progress

[pytest-watch]
ext=py,feature